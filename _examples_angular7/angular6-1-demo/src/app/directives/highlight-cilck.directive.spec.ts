import { HighlightCilckDirective } from './highlight-cilck.directive';

describe('HighlightCilckDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCilckDirective();
    expect(directive).toBeTruthy();
  });
});
