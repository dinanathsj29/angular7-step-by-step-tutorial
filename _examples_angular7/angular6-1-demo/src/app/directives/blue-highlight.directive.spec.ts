import { BlueHighlightDirective } from './blue-highlight.directive';

describe('BlueHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BlueHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
